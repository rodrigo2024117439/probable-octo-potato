# Package: pt.isec.pa.deepsea.ui

**Descrição:**
Implementa a interface gráfica em modo JavaFX.
Contém as classes que desenham o jogo e gerem a interação do utilizador, atuando apenas na camada visual sem incluir validações de estado do jogo.